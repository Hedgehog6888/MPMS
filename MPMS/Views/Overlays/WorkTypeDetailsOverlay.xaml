<UserControl x:Class="MPMS.Views.Overlays.WorkTypeDetailsOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#6B778C"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#8993A4"/>
            <Setter Property="Margin" Value="0,20,0,10"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Шапка в стиле админки -->
        <Border Grid.Row="0" Padding="24,18"
                BorderBrush="#E8E9EC" BorderThickness="0,0,0,1" Background="White">
            <Grid>
                <StackPanel>
                    <TextBlock x:Name="TitleText" FontSize="20" FontWeight="Bold" Foreground="#000000"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="CategoryText" FontSize="12" Foreground="#6B778C" Margin="0,4,0,0"/>
                </StackPanel>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="Close_Click" Width="32" Height="32">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="#F4F5F7" CornerRadius="16">
                                <TextBlock Text="✕" FontSize="14"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Foreground="#6B778C"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#E8E9EC"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Контент -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,20,24,8">

                <TextBlock Text="ОСНОВНАЯ ИНФОРМАЦИЯ" Style="{StaticResource SectionHeader}" Margin="0,0,0,10"/>

                <!-- Артикул -->
                <TextBlock Text="Артикул" Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="ArticleText" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="Цена" Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="PriceText" FontSize="20" FontWeight="Bold" Foreground="#000000" Margin="0,0,0,16"/>

                <TextBlock Text="Описание" Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="DescriptionText" FontSize="13" Foreground="#374151" TextWrapping="Wrap" Margin="0,0,0,16">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="СИСТЕМНАЯ ИНФОРМАЦИЯ" Style="{StaticResource SectionHeader}"/>

                <!-- Даты -->
                <Border Background="#F4F5F7" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock x:Name="CreatedText" FontSize="12" Foreground="#64748B"/>
                        <TextBlock x:Name="UpdatedText" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Секция истории цен -->
                <Border x:Name="PriceHistoryPanel"
                        Background="#F4F5F7" CornerRadius="8" Padding="12"
                        Visibility="Visible">
                    <StackPanel>
                        <!-- Заголовок с названиями колонок -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="ИСТОРИЯ ЦЕН" FontSize="11" FontWeight="SemiBold"
                                       Foreground="#8993A4"/>
                            <TextBlock Grid.Column="1" FontSize="11" Foreground="#94A3B8" Margin="8,0,30,0" Text="Старая"/>
                            <TextBlock Grid.Column="2" FontSize="11" Foreground="#94A3B8" Margin="8,0,0,0" Text="Новая"/>
                        </Grid>
                        <ItemsControl x:Name="PriceHistoryList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" FontSize="12" Foreground="#64748B"
                                                   Text="{Binding ChangedAt, StringFormat=dd.MM.yyyy HH:mm}"/>
                                        <TextBlock Grid.Column="1" FontSize="12" Foreground="#EF4444"
                                                   Margin="8,0,8,0"
                                                   Text="{Binding OldPrice, StringFormat={}{0:N2} ₽}"/>
                                        <TextBlock Grid.Column="2" FontSize="12" Foreground="#10B981"
                                                   Margin="8,0,0,0"
                                                   Text="{Binding NewPrice, StringFormat={}{0:N2} ₽}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock x:Name="NoHistoryText" FontSize="12" Foreground="#94A3B8"
                                   Text="История изменений цены отсутствует" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопка действия в стиле админки -->
        <Border Grid.Row="2" Padding="24,14"
                BorderBrush="#E8E9EC" BorderThickness="0,1,0,0" Background="White">
            <Button Content="Редактировать"
                    Style="{StaticResource BlackBtn}"
                    HorizontalAlignment="Right"
                    Click="Edit_Click"/>
        </Border>
    </Grid>
</UserControl>
