<UserControl x:Class="MPMS.Views.Components.ProjectManagementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:infra="clr-namespace:MPMS.Infrastructure">
    <UserControl.Resources>
        <infra:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    
    <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,8"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            Visibility="{Binding Project.IsClosed, Converter={StaticResource BoolToVisInv}}">
        <StackPanel>
            <TextBlock Text="Управление проектом" FontSize="14" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
            <StackPanel>

                <Button x:Name="EditProjectBtn" Click="EditProject_Click"
                        Height="34" Padding="12,0" Margin="0,0,0,8" FontSize="13"
                        ToolTipService.ShowOnDisabled="True">
                    <Button.ToolTip>
                        <ToolTip>
                            <TextBlock Text="Сначала снимите пометку удаления"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="ToolTipService.IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Project.IsMarkedForDeletion}" Value="True">
                                    <Setter Property="ToolTipService.IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="White" CornerRadius="6"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{StaticResource TextPrimaryBrush}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BackgroundBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Content>Редактировать</Button.Content>
                </Button>

                <Button x:Name="MarkProjectBtn" Click="MarkProject_Click"
                        Height="34" Padding="12,0" Margin="0,0,0,8" FontSize="13"
                        Visibility="Collapsed">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="White" CornerRadius="6"
                                    BorderBrush="#EF4444" BorderThickness="1"
                                    Padding="{TemplateBinding Padding}">
                                <TextBlock x:Name="MarkBtnText" Text="Пометить к удалению"
                                           Foreground="#EF4444" FontSize="13"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#FEF2F2"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="CloseProjectBtn" Click="CloseProject_Click"
                        Height="34" Padding="12,0" FontSize="13"
                        ToolTipService.ShowOnDisabled="True">
                    <Button.ToolTip>
                        <ToolTip>
                            <TextBlock Text="Сначала снимите пометку удаления"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="ToolTipService.IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Project.IsMarkedForDeletion}" Value="True">
                                    <Setter Property="ToolTipService.IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="Black" CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="White"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#333"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Content>Закрыть проект</Button.Content>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
